#include <stdio.h>

int main(){
    int a =2 , b=9;
    int sum =a+b ;
    int subtraction = a-b;
    int product = a*b;
    int div =a/b;
    int mod =a%b;
    printf("operators in C and the value of sum is %d\n",sum);
    printf("operators in C and the value of difference is %d\n",subtraction);
    printf("operators in C and the value of product is %d\n",product);
    printf("operators in C and the value of division is %d\n",div);
    printf("operators in C and the value of modulus is %d\n",mod);
    return 0;
}