#include <stdio.h>

int main(){
    int x=3; // initialise the value of x to 3
    printf("The value of x is %d",x); // print the value of x using printf and %d as format specifier for integer
    return 0;
}